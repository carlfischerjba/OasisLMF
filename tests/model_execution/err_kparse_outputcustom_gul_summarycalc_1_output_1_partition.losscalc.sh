#!/bin/bash
set -euET -o pipefail
shopt -s inherit_errexit 2>/dev/null
( custom_gulcalc > fifo/gul_P$1  ) 2>> $2/stderror.err &
#!/bin/bash
set -euET -o pipefail
shopt -s inherit_errexit 2>/dev/null
( custom_gulcalc > fifo/gul_P$1  ) 2>> $2/stderror.err &
#!/bin/bash
set -euET -o pipefail
shopt -s inherit_errexit 2>/dev/null
( custom_gulcalc > fifo/gul_P$1  ) 2>> $2/stderror.err &
#!/bin/bash
set -euET -o pipefail
shopt -s inherit_errexit 2>/dev/null
( custom_gulcalc > fifo/gul_P$1  ) 2>> $2/stderror.err &
#!/bin/bash
set -euET -o pipefail
shopt -s inherit_errexit 2>/dev/null
( custom_gulcalc > fifo/gul_P$1  ) 2>> $2/stderror.err &
#!/bin/bash
set -euET -o pipefail
shopt -s inherit_errexit 2>/dev/null
( custom_gulcalc > fifo/gul_P$1  ) 2>> $2/stderror.err &
